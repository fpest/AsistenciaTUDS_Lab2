
extends layout
block content

    html 

        head 
        body
                - fechahoy = hoy
                - usuario = user
                if (mensaje)
                    - mensajeRegistro = mensaje 
                else 
                    - mensajeRegistro = ""
                

                H2= `Bienvenido ${user.nombre} (${user.rol})` 
                div(class='dropdown')
                    button(class='dropbtn') Materias
        
                        div(class="dropdown-content")
                            a(href='/materias/antesInsertar') Alta
            
                div(class='dropdown1')            
                 button(class='dropbtn1') Profesores
                 
                        div(class="dropdown-content1")
                            a(href="#") Alta
            
                div(class='dropdown3')            
                 button(class='dropbtn3') Informes
                 
                        div(class="dropdown-content3")
                            a(href="#") Asistencias
                
                div(class='dropdown2')            
                 button(class='dropbtn2') Salir
                 
                        div(class="dropdown-content2")
                            a(href='/coordinadores/principalCoordinadores') Volver
                            a(href='/login/logout') Logout
                           
        Br
        Br

                H2 Alta de Profesor
                    
                h3=mensajeRegistro

                form(action='/profesores/insertarRegistroProfesor', method="post", id="formulario")


                    input(type="text", id="nombre", name="nombre", required)
                    label :Nombre
                    br
                    
                    input(type="text", id="apellido", name="apellido", required)
                    label :Apellido
                    br
                
                    input(type="text", id="dni", name="dni", required)
                    label :DNI
                    br

                    input(type="email", id="eMail", name="eMail", required)
                    label :Usuario *
                    br
                
                    input(type="password", id="pss", name="pss", required)
                    label :Password 
                                   
                    p * Usuario: Debe ser un email!
                    br
                
                    input(class="boton", type="submit", value="Registrar")

                    br
                    br




                form(id="formuIns")

                    input(class="botonLO", type="button", value="Salir", name="salir" onclick="salirRegistro()")  
            



        script.
            function salirRegistro(){
                console.log("salir")
                const form = document.getElementById('formuIns');
                form.setAttribute('method', 'get');
                form.setAttribute('action','../coordinadores/principalCoordinadores');

                form.submit();
            }

            function error(){
                console.log("error")
                alert("Error")
            }

            

     
           
          
    